import { IColumn } from './column-interface';

export interface IBoardState {
    id: string;
    sprintId: string;
    productId?: string;
    productName: string;
    sprintName: string;
    columns: IColumn[];
}
